services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: pension_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pension_pgdata:/var/lib/postgresql/data
volumes:
  pension_pgdata:
